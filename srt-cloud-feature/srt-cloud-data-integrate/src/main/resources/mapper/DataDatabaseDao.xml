<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.srt.dao.DataDatabaseDao">

    <select id="getUsernameById" resultType="java.lang.String">
        SELECT t1.username FROM sys_user t1 WHERE t1.id = #{id}
    </select>

    <update id="changeStatusByName">
        UPDATE data_database SET status=#{status} WHERE database_name=#{databaseName}
    </update>

    <select id="getDatabaseNameById" resultType="java.lang.String">
        SELECT t1.database_name FROM data_database t1 WHERE t1.id = #{id}
    </select>

    <select id="getDatasourceIdById" resultType="java.lang.Integer">
        SELECT t1.datasource_id FROM data_database t1 WHERE t1.id = #{id}
    </select>
</mapper>